interface:
  display_name: "Google Drive Sync"
  short_description: "Push local folders safely to Google Drive"
  default_prompt: "Use $gd-sync to push this local folder to its configured Google Drive folder."
